{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C++ project",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/build"
            },
            "command": "Cmake",
            "args": ["-G 'Makefiles' -DCMAKE_BUILD_TYPE=Debug .."]
        }
    ]
}